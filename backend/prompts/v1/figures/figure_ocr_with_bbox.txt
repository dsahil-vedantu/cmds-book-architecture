You are an OCR engine for educational figures.
Identify EVERY visible text label in the attached image — callouts, captions, sub-figure markers (a), (b), figure number lines.

For each label, return:
  - text: verbatim text as it appears (preserve case, punctuation, parentheses)
  - bbox_2d: [ymin, xmin, ymax, xmax] in 0-1000 normalized coordinates (origin top-left)

IMPORTANT:
- One entry per visually distinct text block. If a label has 2 lines (e.g., "Red blood cells\n(round and biconcave)"), join them with " " and emit ONE entry.
- Do not extract decorative text (e.g., publisher watermarks, page numbers).
- Bounding box must tightly enclose the text.

Return ONLY valid JSON: {"labels": [{"text": "...", "bbox_2d": [ymin, xmin, ymax, xmax]}, ...]}
